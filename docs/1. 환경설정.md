## AI Tutor Phase 4: 프론트엔드 환경설정

### 1. 프로젝트 생성

```bash
mkdir ai-tutor-poc
cd ai-tutor-poc
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

### 2. shadcn/ui

```bash
npx shadcn@latest init
npx shadcn@latest add button input card avatar badge scroll-area separator sheet skeleton tabs
```

### 3. Supabase Auth + Client

```bash
npm install @supabase/supabase-js @supabase/ssr
```

### 4. KaTeX (수식 렌더링)

```bash
npm install katex react-katex
npm install -D @types/katex @types/react-katex
```

### 5. 기타

```bash
npm install react-markdown lucide-react
```

### 6. 환경 변수

```bash
cp .env.example .env.local
```

`.env.local`:
```
NEXT_PUBLIC_SUPABASE_URL=https://muefuihjjihdihzjpvbk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<supabase-anon-key>
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://n8n.srv812064.hstgr.cloud/webhook/<production-id>
NEXT_PUBLIC_MEM0_API_URL=http://193.168.195.222:8888
```

### 7. 디렉토리 구조

```bash
mkdir -p src/lib
mkdir -p src/components/chat
mkdir -p src/components/landing
mkdir -p src/components/ui
mkdir -p src/app/chat
```

```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx              # 랜딩 (/ )
│   └── chat/
│       └── page.tsx          # 채팅 (/chat)
├── components/
│   ├── chat/
│   │   ├── chat-panel.tsx    # 채팅 영역
│   │   ├── chat-bubble.tsx   # 메시지 버블
│   │   ├── chat-input.tsx    # 입력창
│   │   ├── katex-renderer.tsx # 수식 렌더링
│   │   └── quick-actions.tsx # 추천 질문 버튼
│   ├── landing/
│   │   ├── hero.tsx          # 히어로 섹션
│   │   ├── compare-demo.tsx  # 비교 데모
│   │   ├── architecture.tsx  # 아키텍처 설명
│   │   ├── student-cards.tsx # 데모 계정 카드
│   │   └── scenarios.tsx     # 추천 시나리오
│   └── ui/                   # shadcn 컴포넌트
├── lib/
│   ├── supabase/
│   │   ├── client.ts         # 브라우저 클라이언트
│   │   ├── server.ts         # 서버 클라이언트
│   │   └── middleware.ts     # auth guard
│   ├── api.ts                # n8n webhook, mem0 호출
│   └── types.ts              # 타입 정의
└── middleware.ts              # auth redirect
```

### 8. 빌드 확인

```bash
npm run build
```